auth:
  storage_path: ${LINKEDIN_AUTH_STORAGE_PATH}
  email: ${LINKEDIN_EMAIL}
  password: ${LINKEDIN_PASSWORD}

qna:
  storage_path: ${LINKEDIN_QNA_STORE_PATH}

search:
  time_range: past24h
  job_limit: 1
  position: Backend developer
  location: Indonesia
  company_blacklist:
    - Diksha
    - Super indo
  remote_filter:
    - onsite
    - remote
    - hybrid
  experience:
    - intern
    - entry
    - mid-senior
